# گالری نوول — راهنمای بالا آوردن لوکال (روی سیستم خودتان)

این پوشه شامل دو بخش است:
```
nouvelle-gallery-project/
├── backend/    سرور Node.js + Express + MongoDB
└── frontend/   فایل index.html که به بک‌اند وصل می‌شود
```

## مرحله ۱ — نصب MongoDB
یکی از این دو راه را انتخاب کنید:
- **ساده‌ترین راه:** یک حساب رایگان در [MongoDB Atlas](https://www.mongodb.com/atlas) بسازید و آدرس اتصال (connection string) بگیرید.
- یا MongoDB را روی سیستم خودتان نصب کنید: https://www.mongodb.com/try/download/community

## مرحله ۲ — بالا آوردن بک‌اند
```bash
cd backend
npm install
cp .env.example .env
```
فایل `.env` را باز کنید و حداقل این مقادیر را تنظیم کنید:
- `MONGO_URI` → آدرس دیتابیستان
- `JWT_SECRET` → یک رشته‌ی دلخواه و طولانی
- `ADMIN_USERNAME` و `ADMIN_PASSWORD` → اطلاعات ورود پنل مدیریت

سپس:
```bash
npm run seed     # ساخت کاربر مدیر و ثبت نرخ اولیه طلا (فقط بار اول)
npm run dev      # اجرای سرور روی http://localhost:4000
```

## مرحله ۳ — باز کردن فرانت‌اند
فایل `frontend/index.html` را مستقیم با دوبار کلیک در مرورگر باز کنید.

⚠️ در ابتدای فایل `frontend/index.html` مقدار `API_BASE` باید با آدرس بک‌اند یکی باشد (پیش‌فرض: `http://localhost:4000`).

## نکته درباره اجرت
در پنل مدیریت، هنگام افزودن محصول، اجرت به‌صورت **درصد** وارد می‌شود (مثلا 12 یعنی ۱۲٪)، نه مبلغ ثابت. فرمول قیمت‌گذاری کامل:
```
قیمت طلا = وزن × نرخ روز طلای ۱۸ عیار
اجرت     = درصد اجرت وارد شده × قیمت طلا
سود      = ۷٪ از (قیمت طلا + اجرت)
مالیات   = ۱۰٪ از (اجرت + سود)
قیمت نهایی = قیمت طلا + اجرت + سود + مالیات
```

## نکته درباره پرداخت
تا وقتی `ZARINPAL_MERCHANT_ID` واقعی را در `.env` بک‌اند نگذاشته‌اید، سفارش‌ها بدون درگاه واقعی پرداخت ثبت می‌شوند. جزییات در `backend/README.md`.

## تم بصری
تم سایت روشن (عاجی/سفید) با لهجه‌ی طلایی است. رنگ‌ها همگی به‌صورت متغیرهای CSS در ابتدای فایل `frontend/index.html` (داخل `:root`) تعریف شده‌اند، پس اگر خواستید رنگ‌ها را باز هم تغییر دهید فقط کافی‌ست همان‌جا را ویرایش کنید.

موفق باشید! 🌟
